<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ru.ursmu.application"
          android:versionCode="2"
          android:versionName="1.0">
    <uses-sdk
            android:targetSdkVersion="15"
            android:minSdkVersion="8"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <supports-screens
            android:xlargeScreens="false"/>

    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher"
                 android:theme="@style/Theme.Sherlock.Light.DarkActionBar">
        <activity android:name="ru.ursmu.application.Activity.MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="ru.ursmu.application.Activity.FindFacultyActivity" android:label="Выберите факультет"/>
        <activity android:name="ru.ursmu.application.Activity.FindKursActivity" android:label="Выберите курс"/>
        <activity android:name="ru.ursmu.application.Activity.FindGroupActivity" android:label="Выберите группу"/>
        <activity android:name="ru.ursmu.application.Activity.GroupScheduleActivity" android:label="Группа"/>
        <activity android:name="ru.ursmu.application.Activity.ProfessorScheduleActivity" android:label="Профессор"/>
        <activity android:name="ru.ursmu.application.Activity.NewsActivity" android:label="Новости УГГУ"/>
        <activity android:name="ru.ursmu.application.Activity.GrornyTVActivity" android:label="Горные Вести"/>-

        <service android:name="ru.ursmu.application.Activity.UrsmuService"/>
    </application>
</manifest>
